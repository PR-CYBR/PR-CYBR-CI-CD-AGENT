{
    "discord": {
      "token": "${DISCORD_TOKEN}",
      "channel": "${DISCORD_CHANNEL_NAME}"
    },
    "git": {
      "token": "${GIT_TOKEN}"
    },
    "slack": {
      "token": "${SLACK_TOKEN}",
      "channel": "${SLACK_CHANNEL_NAME}"
    }
  }